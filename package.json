{
  "name": "driftwatch",
  "version": "0.1.0",
  "private": true,
  "description": "API dependency change monitor — crawls changelogs, classifies breaking changes with AI, alerts engineering teams",
  "scripts": {
    "dev:web": "pnpm --filter @driftwatch/web dev",
    "dev:api": "pnpm --filter @driftwatch/api start:dev",
    "build:web": "pnpm --filter @driftwatch/web build",
    "build:api": "pnpm --filter @driftwatch/api build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter @driftwatch/api prisma:migrate",
    "db:generate": "pnpm --filter @driftwatch/api prisma:generate"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "unrs-resolver",
      "esbuild"
    ]
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
